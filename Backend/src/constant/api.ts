import { Role } from "../entity/Role";

const action = {
  ADD: '/add',
  LIST: '/list',
  UPDATE: '/update',
  DELETE: '/delete',
  DETAIL: '/detail',
  LOGIN: '/login',

};

const component = {
  USER: 'user',
  ROLE: 'role',
};



export {  component, action,  };